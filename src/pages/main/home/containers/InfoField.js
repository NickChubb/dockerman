import React from 'react';
import '../index.css';

const InfoField = ({ title }) => {
    return (
        <div className="info-field">
            <h3>{title}</h3>
        </div>
    )
}

export default InfoField;
